#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a ;
	int b ;

	for (a = 48; a < 58; a++)
	{
		for (b = 49; b < 58; b++;)
		{
			if ( a1 != a2 && a1 < a2)
			{
				putchar(a1);
				putchar(a2);
				if (a1 == 56 && a2 == 57)
				{
					break;
				}
				putchar(',');
				putchar(' ');
			}
		}
	}
	return (0);
}
