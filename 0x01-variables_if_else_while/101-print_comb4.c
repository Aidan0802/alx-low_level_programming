#include <stdio.h>

/**
 * main - Entry Point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a ;
	int b ;
	int c ;

	for (c = 48; c < 58; c++)
	{
		for (a = 49; a < 58; a++)
		{
			for (b = 50; b < 58; b++)
			{
				if ( a1 != a2 && a1 < a2)
				{
					putchar(a1);
					putchar(a2);
				if (a1 == 56 && a2 == 57)
				{
					break;
				}
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	return (0);
}
