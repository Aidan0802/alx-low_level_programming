#include <stdio.h>

/**
 * main - prints the name of program
 *
 * @argv: argument value
 * @argc: argument count
 *
 * Return: Always 0 (Success)
 */

int main(int argc, char *argv[])
{
	argc = 0;

	printf("%s\n", argv[argc]);

	return (0);

}
